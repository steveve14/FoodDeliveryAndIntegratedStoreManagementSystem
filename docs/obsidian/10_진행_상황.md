---
tags:
  - 진행현황
  - 타임라인
  - 변경이력
관련:
  - "[[11_우선순위_백로그]]"
---

# 10. 진행 현황 타임라인

---

## 📅 작업 이력

### 2026-03-10 — 카테고리 & 프론트 연동 점검

- `service-store` 카테고리 처리 로직 코드 정규화 기반으로 확장
  - 한글/영문 혼용 입력(`치킨`, `CHICKEN`) → 표준 코드 변환
  - `SNACK`, `NIGHT`, `BOSSAM`, `ASIAN`, `SALAD`, `LUNCHBOX` 카테고리 프로필 확장
- `web-user` 카테고리 확장
  - 기존: 치킨/한식/분식/일식/디저트
  - 추가: 중식/양식/피자/버거/족발보쌈/야식/아시안/샐러드/도시락/카페
- 백엔드-프론트 연동 점검 (컴파일 성공, 기동 확인)
- 하드코딩 고위험 항목 전수 점검 (JWT secret, 배달비, VIP 기준, gRPC 주소)

---

### 2026-03-13 — 실연동 차단요소 해소 (P0-0 완료)

- `GET /api/v1/stores` 등 3개 공개 API 비인증 처리
- SSR 쿠키 삭제 버그 수정 (`useApi.ts` — `if (import.meta.client)` 가드)
- Gateway JWT secret 정렬
- **Playwright E2E 14단계 시나리오 통과** (`1 passed (4.1s)`)
  - 홈 → 카테고리 필터 → 매장 상세 → 장바구니 → 결제 → 주문 내역
- Gateway CORS 개선: 포트 3010, 3100, 3200 추가

---

### 2026-03-15 — Spring gRPC GA 업그레이드 & Android 스캐폴드

- **Spring gRPC 1.0.0-RC1 → 1.0.2 GA** (Maven Central 정식 배포)
  - io.grpc: 1.76.0 → 1.77.1
  - Protobuf: 4.32.1 → 4.33.2
- **Android 앱 스캐폴드 4종 추가**
  - `app-android-shop`, `app-android-user`, `app-android-kiosk`, `app-android-delivery`
  - Android SDK 34, minSdk 24, Material Design, ConstraintLayout
- 전체 문서 코드 기준으로 최신화

---

### 2026-03-17 — Mock 제거 & DB 시드 확장

- 프론트 주요 mock 제거
  - `web-admin`: 운영/마케팅/시스템 주요 화면 mock 제거
  - `web-shop`: 배달 대기 주문 더미 제거
  - `web-user`: `HomeSales`, `HomeStats`, `HomeChart.client` 랜덤/더미 제거
- DB 시드 확장 (유형별 5건)
  - user / store / order / delivery / event 각 5건

---

### 2026-03-30 — API 표준 통일 & Docker 인프라 (P0-2 완료)

- API 표준 통일 100% 완료
  - 전 서비스 `RestExceptionHandler` 에러 코드 정책 통일
  - 전 서비스 `NoSuchElementException` → 404 핸들러 추가
- Android 앱 API 연동 개선
  - `app-android-delivery`: `DeliveryDto` 모델 추가, 배달 전용 API 연동
  - `app-android-kiosk`: 하드코딩 스토어 ID → `KioskConfig` (SharedPreferences) 교체
- **Docker 인프라 구성 완료**
  - `Dockerfile` (멀티 스테이지, `ARG SERVICE_NAME` 공용화)
  - `docker-compose.yml` (8개 서비스 + PostgreSQL + Redis)
  - DB 초기화 스크립트 작성
  - 시드 데이터 자동화 스크립트

---

### 2026-04-05 — 보안 하드닝 1차 (P0-6 완료)

- **서비스 내부 JWT 재검증** 추가 (user/store/order/delivery/event)
- `X-User-Id`, `X-User-Role` 헤더 ↔ 토큰 클레임 대조 검증
- 민감 엔드포인트 인가 강화 (주문/배달/이벤트/사용자)
- Auth 쿠키 보안 강화
  - `ResponseCookie` 방식으로 변경
  - `SameSite=Lax` 적용, `Secure` 프로파일 분리
- **Refresh Token 평문 → SHA-256 해시 저장** 전환
- `docker-compose.yml`에 내부 서비스용 `TOKEN_SECRET` 전달 반영

---

### 2026-04-06 — 서비스 기동 안정성 보강

- 서비스 기동 안정성 추가 보강 (상세 내용은 `docs/10_Current_Progress_Status.md` 참조)

---

## 📈 완료율 추적

| 카테고리 | 완료 | 전체 |
|---|---|---|
| P0 핵심 과제 | 7 | 7 |
| P1 핵심 기능 | 진행 중 | ~5 |
| P2 운영/확장 | 미착수 | ~5 |

---

## 🔗 연관 문서

- [[11_우선순위_백로그]] - 남은 작업 목록
- [[09_보안_현황]] - 보안 하드닝 상세
- [[00_대시보드]] - 전체 요약

#진행현황 #타임라인 #변경이력
