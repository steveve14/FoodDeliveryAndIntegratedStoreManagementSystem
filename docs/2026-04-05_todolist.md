# 2026-04-05 수정 필요 TODO

## 작성 목적

- 2026-04-05 보안 수정 사항을 기준으로 `docs/` 폴더에서 **즉시 수정이 필요한 문서**와 **추가 작성이 필요한 문서**를 순서대로 정리한다.
- 이번 TODO는 **실제 코드 반영 완료 상태**를 문서에 맞추는 것이 목표다.

## 1. 최우선 수정 대상

- [ ] `docs/10_Current_Progress_Status.md` 최신화
  - 2026-04-05 보안 보강 반영 내용 추가
  - 서비스별 JWT 직접 검증 추가 (`service-user`, `service-store`, `service-order`, `service-delivery`, `service-event`)
  - order/delivery/user frontend 민감 조회 API 보호 범위 변경 반영
  - auth 쿠키 속성(`Secure`, `SameSite`) 환경별 설정 반영
  - refresh token 평문 저장 제거 및 해시 저장 방식 반영
  - `docker-compose.yml` 에서 내부 서비스 `TOKEN_SECRET` 전달 추가 반영

- [ ] `docs/11_Priority_Worklist.md` 최신화
  - 보안 관련 우선순위 작업 상태를 현재 코드 기준으로 재정렬
  - 완료 항목으로 반영할 것:
    - 하위 서비스 헤더 신뢰 축소 및 JWT 직접 검증
    - 주문/배달 상세 조회 보호 강화
    - 사용자 frontend 운영 데이터 엔드포인트 관리자 제한
    - refresh token 저장 방식 개선
  - 남은 후속 과제로 반영할 것:
    - `customer-summaries` 매장 단위 테넌트 범위 제한
    - 내부 서비스 포트 외부 비노출 구조 정리

## 2. 보안 문서 정리

- [ ] `docs/13_Security_Audit.md` 최신화
  - 기존 감사 결과 중 이번에 실제 수정 완료된 항목 상태 변경
  - 더 이상 미수정이 아닌 항목으로 이동할 것:
    - IDOR 일부 개선 (`orders/{id}`, `deliveries/{id}`)
    - EventController 권한 미적용 문제
    - frontend 운영 데이터 무인가 문제
    - 쿠키 보안 속성 미흡 문제 일부 개선
    - refresh token 평문 저장 문제 해결
  - 토이 프로젝트 정책상 제외한 비밀정보 하드코딩 항목은 유지하되, 범위를 명확히 구분

- [ ] `docs/15_Security_Analysis_2026-03-29.md` 후속 업데이트 또는 후속 문서 추가
  - 2026-03-29 분석 내용 중 2026-04-05에 해결된 항목과 미해결 항목을 분리
  - 미해결 항목만 남기고, 해결 항목은 `완료` 섹션으로 이동
  - 특히 아래 항목 상태 업데이트 필요:
    - 하위 서비스 직접 접근 시 헤더 스푸핑 위험 완화 여부
    - event/order/delivery 엔드포인트 인가 보강 여부
    - cookie 속성 강화 여부

## 3. 신규 문서 추가 권장

- [ ] `docs/2026-04-05-security-hardening-summary.md` 신규 작성
  - 이번 보안 패치의 변경 배경, 수정 파일 범위, 사용자 영향, 배포 시 주의사항 요약
  - 반드시 포함할 내용:
    - 기존 refresh token 무효화 가능성
    - 운영 환경에서 `TOKEN_SECRET` 주입 필요
    - local/profile 별 쿠키 secure 설정 차이
    - 서비스 직접 포트 접근 정책 재검토 필요

- [ ] `docs/07_Development_Environment.md` 보강
  - 로컬 실행 시 `TOKEN_SECRET` 이 auth 외 서비스에도 필요하다는 점 반영
  - docker-compose / 개별 서비스 실행 시 필요한 환경변수 표 정리
  - 보안 검증 흐름(게이트웨이 우회 테스트 금지, 직접 포트는 개발용임)을 명시

## 4. API/연동 문서 보강

- [ ] `docs/FRONTEND_API_LIST.md` 최신화
  - 권한 요구사항이 바뀐 API 반영
  - 최소 반영 대상:
    - `GET /api/v1/orders/{id}`
    - `GET /api/v1/deliveries/{id}`
    - `GET /api/v1/orders/frontend/customer-summaries`
    - `GET /api/v1/users/frontend/*`
  - 각 API에 필요한 역할 또는 인증 조건을 명시

- [ ] `docs/06_API_Design_Standards.md` 보강
  - Gateway 주입 헤더만 신뢰하지 않고, 서비스 자체 토큰 검증을 수행하는 정책 반영
  - 민감 조회 API에는 역할 검증 + 소유권 검증을 병행한다는 규칙 명시

## 5. 후순위 검토 대상

- [ ] `docs/13_Security_Audit.md` 와 `docs/15_Security_Analysis_2026-03-29.md` 간 중복 내용 정리
  - 감사 보고서와 상세 분석 보고서 역할을 분리
  - 감사 문서는 현재 상태 중심, 상세 분석은 근거/사례 중심으로 정리

- [ ] `docs/index` 또는 문서 목차 계열 파일에 신규 문서 링크 반영 여부 검토
  - `2026-04-05-security-hardening-summary.md` 를 추가하면 색인에도 연결

## 실행 순서 권장

1. `docs/10_Current_Progress_Status.md`
2. `docs/11_Priority_Worklist.md`
3. `docs/13_Security_Audit.md`
4. `docs/15_Security_Analysis_2026-03-29.md`
5. `docs/2026-04-05-security-hardening-summary.md`
6. `docs/07_Development_Environment.md`
7. `docs/FRONTEND_API_LIST.md`
8. `docs/06_API_Design_Standards.md`

## 메모

- 이번 TODO는 **비밀정보 하드코딩 이슈를 제외한 보안 수정 사항만** 기준으로 작성한다.
- 문서 업데이트 시 `완료`, `진행 중`, `후속 필요`를 명확히 구분해서 상태 혼선을 줄인다.