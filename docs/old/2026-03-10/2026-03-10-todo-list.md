# 2026-03-10 수정 필요 TODO

## 2026-03-13 업데이트
- [x] 백엔드 테스트 안정화: WebMvcTest/REST Docs import 이슈 수정 후 전체 서비스 테스트 성공
- [x] 게이트웨이 stores 정책 확정: 조회 GET 공개, 관리/변경 API 보호

## 1. 카테고리/사용자 앱
- [x] `web-user` 카테고리 목록을 음식앱 기준으로 확장
- [x] 백엔드 카테고리 표준화(한글/영문 입력 정규화)
- [x] 관리자/점주 앱 카테고리 선택 UI도 동일 카테고리 세트로 통일
- [x] DB 시드에 신규 카테고리(`SNACK`, `NIGHT`, `BOSSAM`, `ASIAN`, `SALAD`, `LUNCHBOX`) 샘플 매장 추가

## 2. 프론트-백엔드 연동
- [x] `web-user` dev 서버 실행 확인 (`http://localhost:3010`)
- [x] 백엔드 게이트웨이 응답 확인 (`/api/v1/stores` 현재 401)
- [x] `stores` 목록 API 공개/보호 정책 최종 확정
- [x] 정책 확정에 맞춰 gateway route 필터/프론트 인증 플로우 조정
	- 정책: `GET /api/v1/stores`, `GET /api/v1/stores/{id}`, `GET /api/v1/stores/{storeId}/menus` 공개
	- 그 외 `POST/PATCH/DELETE /api/v1/stores/**`는 게이트웨이 인증 필터 유지

## 3. 하드코딩 점검(우선순위)
- [x] gateway JWT secret 환경변수화
- [x] delivery 고정 배달비 상수(`3000`) 설정값 분리
- [x] order VIP 기준(`10`) 설정값 분리
- [x] gRPC `static://127.0.0.1:*` 주소 환경변수화
- [x] Eureka/CORS localhost 하드코딩 설정화

## 4. 품질/검증
- [x] `:service-store:compileJava` 성공
- [x] 수정 파일(`useOrdering.ts`) lint 경고 없음
- [x] `web-user` 전체 lint 기존 오류 정리

- [x] E2E 시나리오: 홈 진입 -> 카테고리 필터 -> 매장 상세 -> 장바구니 검증
	- 2026-03-13 1차: `http://localhost:3100/api/v1/auth/signup` 호출 시 `502 Bad Gateway` 확인
	- 2026-03-13 2차: discovery/auth/user/store/gateway 기동 후 `signup` API 정상(200) 복구 확인
	- 2026-03-13 3차: `service-order` 추가 기동 후 Playwright E2E(`pnpm e2e`) 1건 통과 확인
	- 결과: 홈 진입 -> 카테고리 필터 -> 매장 상세 -> 장바구니/체크아웃 화면 진입까지 자동 검증 완료

## 5. 문서
- [x] `docs/10_Current_Progress_Status.md` 최신화
- [x] `docs/11_Priority_Worklist.md` 최신화
- [x] `docs/13_Security_Audit.md` 최신화
- [x] 금일 TODO 문서 생성
