# 2026-04-06 TODO

> 이전 파일 통합: `2026-03-13-todo-list.md` / `2026-03-21-todo-list.md` / `2026-04-05_todolist.md`
> 완료 항목은 제외하고, 현재 미완 및 후속 필요 항목만 정리합니다.

---

## P1 — 핵심 기능 완성

### P1-A. Android 앱 실기기 E2E 검증 (진행 중)

- [ ] `app-android-user`: 로그인 → 매장 홈 → 메뉴 담기 → 주문 생성 E2E 실기기 통과
- [ ] `app-android-shop`: 로그인 → 주문 대시보드 → 주문 상태 전이 E2E 실기기 통과
- [ ] `app-android-delivery`: 로그인 → 배달 목록 조회 → 상태 업데이트 E2E 실기기 통과
- [ ] `app-android-kiosk`: 메뉴 조회 → 장바구니 → 주문 생성 E2E 실기기 통과

### P1-B. 프론트엔드 Web 잔여 연동 검토

- [ ] `web-admin` / `web-shop` Mock 잔존 여부 스윕 (실데이터 UI 구동 시 빈 화면/오류 항목)
- [ ] 운영 지표 대시보드 통계 API 정확도 교차 검증

---

## P2 — 안정화 및 보안 후속

### P2-A. 보안 후속 하드닝

- [ ] `GET /api/v1/orders/frontend/customer-summaries` STORE 범위 tenant scoping 구현
  - 현재 STORE 역할 전체 허용, 매장 기준 조회 범위 제한 정책 설계 및 구현 필요
- [ ] Docker Compose 내부 서비스 포트 외부 노출 제거
  - `8010`, `8020`, `8030`, `8040`, `8050` → internal network 전환, Gateway(`8000`)만 외부 공개
- [ ] Gateway 선행 `X-User-*` 헤더 제거 후 재주입 정책 명시적 적용
- [ ] 운영 환경 gRPC TLS 또는 내부망 보호 정책 수립

### P2-B. 배포/운영 표준화

- [ ] Docker 이미지 빌드 및 통합 실행 테스트 (`docker-compose up` 전체 정상 기동 확인)
- [ ] K8s 매니페스트 / 환경변수 전략 정비 (Secret, ConfigMap 분리)
- [ ] 서비스 기동 순서 + `TOKEN_SECRET` 포함 환경변수 실행 스크립트 정리

### P2-C. DB 시드 반영

- [ ] DB 시드 수동 반영 실행
  - 순서: `07_remote_db_user_seed.sql` → `08_remote_db_store_seed.sql` → `09_remote_db_order_seed.sql` → `10_remote_db_delivery_seed.sql` → `11_remote_db_event_seed.sql`
  - 근거: `docs/2026-03-17-mock-data-inventory-and-db-seed-plan.md`
- [ ] 시드 반영 후 최소 검증 쿼리 (user/store/order/delivery/event 신규 ID 존재 확인)

### P2-D. 테스트 체계 강화

- [ ] `web-shop` / `web-admin` 실데이터 기반 Playwright E2E 스크립트 추가
- [ ] `web-user` E2E: 주문 생성 → 상태 전이 → 대시보드 집계 반영 흐름 확인
- [ ] 주문(`service-order`) → 배달(`service-delivery`) 백엔드 상태 전이 연계 통합 테스트 1건 확보

---

## P3 — 품질 고도화 (후순위)

- [ ] Rate Limiting 적용 (로그인, 토큰 갱신 엔드포인트)
- [ ] 감사 로깅 추가 (로그인, 주문 상태 변경, 배달 상태 변경)
- [ ] 비밀번호 복잡도 정책 / 계정 잠금 메커니즘
- [ ] Nuxt 보안 헤더 (CSP, HSTS, X-Frame-Options)
- [ ] Android Cleartext Traffic 운영 분리 / API Base URL `BuildConfig` flavor 분리
- [ ] Redis 비밀번호 / CORS 메서드·헤더 제한 정비
- [ ] JWT 키 회전 전략 설계

---

## 참고: 최근 완료 항목 (2026-04-05 ~ 04-06)

- [x] 전 서비스 JWT 직접 검증 + `X-User-*` 헤더 클레임 일치 검증 추가
- [x] 민감 엔드포인트 권한 강화 (orders/{id}, deliveries/{id}, customer-summaries, users/frontend/**, addresses/**)
- [x] Auth 쿠키 `ResponseCookie` 통일, `SameSite=Lax`, `Secure` 프로파일 분리
- [x] Refresh Token SHA-256 해시 저장 전환
- [x] `docker-compose.yml` 내부 서비스 `TOKEN_SECRET` 전달 추가
- [x] 전 서비스 `application.yml` `token.secret` 기본값 추가 (기동 오류 해소)
- [x] 보안 관련 문서 8종 최신화 (10/11/13/15/요약/07/FRONTEND_API/06_API_Design)
