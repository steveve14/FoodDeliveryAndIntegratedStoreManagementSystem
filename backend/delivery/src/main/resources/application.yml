spring:
  datasource:
    url: jdbc:postgresql://192.168.0.25:5433/m_shop
    username: steve14
    password: 12345678#
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never
